import {Component} from '@angular/core';

@Component({
  selector: 'animation-frame',
  template: `
    <schedulers-and-animations-comp></schedulers-and-animations-comp>
  `
})
export class AnimationFrameComponent {
  // Replaced by a web component in a separate project
}
